import { ComicImagesPipe } from './comic-images.pipe';

describe('ComicImagesPipe', () => {
  it('create an instance', () => {
    const pipe = new ComicImagesPipe();
    expect(pipe).toBeTruthy();
  });
});
